model Student {
  id         Int      @id @default(autoincrement())
  user       User     @unique @relation(fields: [userId], references: [id])
  userId     Int
  bio        String?
  // Bio pode ser fora do MVP se for mto difícil encaixar na parte de perfil do usuário, que terá estatísticas, mas como essa parte n vai ser feita no MVP, então a bio pode ficar aqui inutilizada
  lastLogin  DateTime
  campuses   Campus[]
  meta       Meta     @relation(fields: [metaId], references: [id])
  metaId     Int
  // Gamificação? Não é MVP
  // Recebimento de e-mails? Poderia listar quais tipos de e-mails quer receber. Isso fica nessa tabela?
  // De repente categoria de recebimento de e-mail é fora do MVP
  // Qnd a pessoa vai se inscrever em um curso, mostra um texto que inscreveu, ou abre um modal com a confirmação.
  // Esse modal tb tem um botão em algum lugar que fala para alterar as configurações de recebimento de notificações (encontrar um nome mais curto)
  // Recebimento de notificações no app?
  // Seria legal implementar um whatsapp via API para avisar das coisas
  // No caso, usuário tb pode escolher se quer receber via whats ou não
  // Newsletter (Ativa/Desativa se quer ou não)
}
