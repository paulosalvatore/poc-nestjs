// Quando for se inscrever, precisa fazer login
// Mas o usuário pode ter dúvida se vai pagar
// Aí a gente coloca a frase embaixo:
// O Ocean SEMPRE SERÁ GRATUITO

model Enrollment {
  id         Int      @id @default(autoincrement())
  event      Event    @relation(fields: [eventId], references: [id])
  eventId    Int
  student    Student  @relation(fields: [studentId], references: [id])
  studentId  Int
  attended   Boolean
  // Esse campo pode até ter um trigger que cria um registro de e-mail
  // Uma fila pode ser informada com isso
  // Várias maneiras de automatizar esse processo
  // O aluno já recebe e-mail/notificação de que o certificado saiu
  // Isso vai ser logo após o curso no caso do online e logo
  // que preencherem a chamada no caso do presencial
  meta       Meta     @relation(fields: [metaId], references: [id])
  metaId     Int

  @@unique([eventId, userId], name: "unique_event_user")
}
